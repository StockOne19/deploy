version: '3.8'

services:
  api-server:
    image: ${IMAGE_NAME:-ghcr.io/stockone19/server-app:latest}
    container_name: api-server
    ports:
      - "8080:8080"
    restart: unless-stopped

  # worker-server:
  #   image: ghcr.io/your-org/investment-backend-worker:latest
  #   ports:
  #     - "8082:8082"
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=prod
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8082/actuator/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - api-server
      # - web-app
    restart: unless-stopped